model Review {
  id        Int      @id @default(autoincrement())
  rating    Int
  comment   String
  createdAt DateTime @default(now())
  recipeId  Int
  recipe    Recipe   @relation("RecipeReviews", fields: [recipeId], references: [id])
}